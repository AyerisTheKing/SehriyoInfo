# История изменений / Changelog

## Sehriyo Info Website

Все важные изменения проекта документируются в этом файле.

### Система версионирования:

- **Major (+1.0)**: Глобальные изменения (новая база данных, полная перепись архитектуры, смена движка)
- **Minor (+0.5)**: Новые крупные фичи (новый экран, новый режим, "Быстрый старт", "История игр")
- **Patch (+0.1)**: Исправления багов, правки CSS, текстов, рефакторинг UI

---

## [7.2] - 2026-02-17

### Исправлено (Patch +0.1)

- **Перезагрузка после выхода из аккаунта**: теперь после нажатия кнопки «Выйти» страница автоматически перезагружается, возвращая пользователя в состояние «гостя»
- **Ошибка 406 (Not Acceptable) в консоли**: убран JOIN `supervisor:supervisor_id (...)` из запроса к `classes`, который вызывал ошибку при использовании с `.single()`. Профиль классного руководителя загружается отдельным запросом после получения данных класса
- **Оптимизация авторизации**: добавлено кэширование профиля, чтобы избежать повторных запросов при уже загруженном профиле
- **Ускорен вход в аккаунт**: профиль загружается сразу в `handleSignIn()`, а не только через `onAuthStateChange`

---

## [7.1] - 2026-02-17

### Исправлено (Patch +0.1)

- **Обновление структуры БД**: Создан SQL-скрипт `update_db_schema.sql` для фиксации структуры таблиц под новую систему логинов.
  - Добавлена колонка `username` в таблицу `profiles`.
  - Настроены таблицы `classes`, `student_details`, `parent_students` с правильными внешними ключами и ограничениями уникальности (invite codes).
  - Обновлены политики RLS (Row Level Security) для разрешения регистрации и привязки учеников/родителей.

---

## [7.0] - 2026-02-17

### Исправлено (Patch +0.1)

- **Кнопка «Зарегистрироваться» не была видна**: форма регистрации обрезалась, т.к. `auth-body` не имел прокрутки. Добавлена `max-height: calc(90vh - 60px)` и `overflow-y: auto`.

### Добавлено

- **Глазик для паролей**: кнопка показа/скрытия пароля во всех полях (вход, регистрация, подтверждение). Иконка переключается между «открытым» и «закрытым» глазом.

---

## [6.9] - 2026-02-16

### Изменено (Minor +0.5)

- **Скрыта полоса прокрутки (scrollbar)**: глобально убран scrollbar на всех элементах через CSS (webkit, Firefox, IE/Edge)
- **Переработано окно авторизации**:
  - Убран старый заголовок «Авторизация» с маленькой шапкой
  - Добавлены **табы «Вход» / «Регистрация»** на всю ширину окна с иконками
  - Жёлтый индикатор (`accent-yellow`) под активным табом
  - Кнопка закрытия встроена в панель табов
  - Кнопки «Войти» и «Зарегистрироваться» теперь с иконками
  - Поля ввода: обновлён дизайн (фон `#f8f9fb`, увеличенный border-radius, плавный фокус)
- **Авторизация по логину вместо email**:
  - При регистрации пользователь вводит **логин** (латиница, цифры, точки, подчёркивания)
  - При входе используется **логин + пароль**
  - Email можно будет привязать позже в профиле
  - Внутренне: `username@sehriyo.local` — fake email для Supabase Auth
  - Валидация логина: минимум 3 символа, только `[a-zA-Z0-9._]`

### Удалено

- Поле Email из формы входа и регистрации
- Заголовки `.auth-title` («Вход» / «Регистрация» как H3)
- Ссылки `.auth-switch` / `.auth-link` (заменены табами)

---

## [6.4] - 2026-02-16

### Исправлено (Patch +0.1)

- **Критический баг: кнопки главного меню не работали**: Причина — `SyntaxError: Identifier 'supabase' has already been declared`. SDK Supabase (из CDN) создаёт глобальную `var supabase`, а `script.js` объявлял `let supabase = null` — это вызывало конфликт имён, который полностью блокировал выполнение всего `script.js`.
- **Исправление**: переименована переменная `let supabase` → `let supabaseClient` по всему `script.js` (37 замен). `window.supabase` (ссылка на SDK) не затронута.
- **Добавлен CSS-класс `.hidden`**: правило `display: none !important;` — ранее отсутствовало, хотя использовалось в HTML.

---

## [6.3] - 2026-02-16

### Добавлено (Major +1.0)

- **Возвращён Supabase**: CDN `@supabase/supabase-js@2`, инициализация `createClient` в `script.js`
- **Система авторизации**:
  - Кнопка «Войти» в хедере (иконка + текст)
  - Модальное окно с формами входа и регистрации
  - Переключение между формами (вход ↔ регистрация)
  - Поле выбора роли: Учитель, Ученик, Родитель
  - При роли «Ученик» — показывается поле «Код класса» (инвайт-код)
  - Функции: `handleSignUp()`, `handleSignIn()`, `handleSignOut()`
  - Слушатель `onAuthStateChange` для реактивного обновления UI
  - Сообщения об ошибках и успехе в модале авторизации

- **Система инвайт-кодов**:
  - Ученик при регистрации вводит `student_invite_code` из таблицы `classes`
  - Если код верный — ученик привязывается к классу (`student_details`)
  - При создании ученика генерируется `parent_invite_code` (12 символов)
  - Родитель в ЛК вводит `parent_invite_code` для привязки ребёнка (`parent_students`)

- **Личный кабинет (Dashboard)**:
  - Полноэкранное модальное окно
  - Карточка профиля: аватар, ФИО, роль, email, кнопка «Выйти»
  - **Для ученика**: информация о классе, код для родителя
  - **Для учителя**: список своего класса, количество учеников, код приглашения
  - **Для родителя**: форма привязки ребёнка, список привязанных детей с кликом

- **Реальные запросы к БД**:
  - `loadClassData()` — JOIN `classes`, `profiles`, `student_details` из Supabase
  - `loadTeachersFromDB()` — загрузка учителей из `profiles` + `teacher_subjects`
  - `loadCurrentProfile()` — загрузка профиля текущего пользователя
  - Скрытие контактов (`is_contacts_hidden`): если `true` — email/телефон маскируются для гостей

### Изменено

- Кнопка «Войти» / «Кабинет» в хедере переключается автоматически при авторизации
- `loadClassData()` теперь async и тянет данные из Supabase вместо заглушек
- Footer: добавлена версия `v6.3`

### Новые CSS-стили

- `.header-auth-btn`, `.header-profile-btn` — кнопки авторизации в хедере
- `.auth-message`, `.auth-error`, `.auth-success` — сообщения в форме
- `.dashboard-*` — стили личного кабинета
- `.invite-code-box`, `.invite-code` — блок инвайт-кодов
- `.child-card`, `.children-list` — карточки детей (для родителя)
- `.role-*` — цветные бейджи ролей
- `.form-group select`, `.form-hint` — стили для select и подсказок
- `.form-group-inline`, `.btn-compact` — inline-форма привязки ребёнка
- Адаптивные стили для мобильных (dashboard, auth-кнопки)

---

## [5.3] - 2026-02-16

### Изменено (Minor +0.5)

- **Реальная структура классов школы**: каждый класс имеет индивидуальный набор букв
  - 1: А, Б, В, Г
  - 2: А, Б, В, Г, Д
  - 3: А, В, Г, Д
  - 4: А, Б, В, Г, Д, И
  - 5–8: А, Б, В, Г, Д
  - 9: Б, В, Д, И
  - 10, 11: без букв — отображаются как «10 класс», «11 класс»

### Удалено

- **Все мок-данные «людей»**: убраны фейковые имена учеников, учителей, контактные данные
- **Модал «Профиль ученика»** (`student-profile-modal`) — полностью удалён из HTML и JS
- **Модал «Профиль учителя»** (`teacher-profile-modal`) — полностью удалён из HTML и JS
- Функции: `openStudentProfile()`, `openTeacherProfile()`, `openTeacherProfileFromList()`, `initializeTeacherCards()`
- Список учителей в аккордеоне теперь показывает заглушку «Информация об учителях будет добавлена позже»
- При открытии класса отображается «Список учеников будет добавлен позже» вместо фейковых имён

### Добавлено

- CSS-стили: `.empty-list-message`, `.class-btn-single`
- Кнопка одиночного класса для 10 и 11 (без букв)

---

## [4.8] - 2026-02-16

### Исправлено (Patch +0.1)

- **Кнопка «X» в окне учителей не работала**: добавлены отсутствующие обработчики для `teachers-list-close` (кнопка X) и `teachers-list-overlay` (клик по фону)
- Теперь все 6 модальных окон корректно закрываются по кнопке X, клику по overlay и клавише ESC

---

## [4.7] - 2026-02-16

### Добавлено (Patch +0.1)

- **Заголовок «Добро пожаловать! 👋»** на главной странице над кнопками навигации
- Подзаголовок: «Информационный портал школы Сехрие — выберите раздел для начала»
- Адаптивный размер шрифта через `clamp()`
- Анимация появления `fadeInUp`

---

## [4.6] - 2026-02-16

### Изменено (Minor +0.5)

- **Главная страница очищена и упрощена**:
  - Удалена секция «Добро пожаловать» (Welcome Section)
  - Удалена секция «О школе» (About School Section)
  - Три кнопки навигации (Ученики, Учителя, Администрация) перенесены из бокового меню (sidebar) на главную страницу
  - Каждая кнопка имеет иконку, заголовок, описание и стрелку
  - Плавная анимация появления кнопок с задержкой (stagger effect)
  - Hover-эффекты: подъём, подсветка боковой полосы, сдвиг стрелки

### Удалено

- **Гамбургер-меню** (кнопка ☰) из шапки
- **Боковое меню (Sidebar)**: весь HTML, CSS и JavaScript
- Функции: `initializeSidebar()`, `openSidebar()`, `closeSidebar()`, `handleNavigation()`
- Секция «Добро пожаловать» и связанные CSS-стили
- Секция «О школе» и связанные CSS-стили

### Технические детали

- Добавлена функция `initializeMainNav()` для обработки кликов по кнопкам
- Новые CSS-классы: `.main-nav-container`, `.main-nav-btn`, `.main-nav-icon`, `.main-nav-label`, `.main-nav-desc`, `.main-nav-arrow`
- Синяя шапка с надписью «Sehriyo Info» сохранена

---

## [4.1] - 2026-02-16

### Удалено (Patch +0.1)

- **Система аккаунтов полностью удалена**:
  - Кнопка «Войти» из хедера
  - Модальное окно авторизации (формы логина и регистрации)
  - Supabase конфигурация (`SUPABASE_URL`, `SUPABASE_ANON_KEY`)
  - Функция `initSupabase()`
  - Функция `initializeAuth()` и её вызов
  - Функции: `checkAuthState()`, `handleLogin()`, `handleRegister()`, `updateUIForLoggedInUser()`, `showAuthError()`, `showAuthSuccess()`
  - CDN-скрипт `@supabase/supabase-js@2` из HTML
  - CSS-стили `.login-button` и `.login-button:hover`

---

## [4.0] - 2026-02-14

### Исправлено (Patch +0.1)

- **Гамбургер-меню**: Исправлена ошибка в CSS, из-за которой кнопка меню исчезала при наведении. `var(--accent-color)` заменен на `var(--accent-yellow)`.

---

## [3.9] - 2026-02-14

### Исправлено (Patch +0.1)

- **Исправлена структура HTML**: Удален лишний закрывающий тег `</div>` в блоке модальных окон, который мог нарушать парсинг DOM.
- **Повышена надежность скриптов**: Добавлены проверки существования элементов перед назначением обработчиков событий для кнопок входа и меню.
- **Диагностика**: Добавлено логирование действий пользователя (клик по кнопке входа и гамбургеру) для упрощения отладки.

---

## [3.8] - 2026-02-14

### Добавлено (Minor +0.5)

- **Раздел "Учителя" с сортировкой по предметам**:
  - Полноэкранное окно со списком всех учителей
  - Accordion с 12 предметами:
    - 📐 Математика
    - 📖 Русский язык и литература
    - 🌍 География
    - 📜 История
    - 🔬 Физика
    - 🧪 Химия
    - 🌿 Биология
    - 🌐 Английский язык
    - 💻 Информатика
    - 🎨 ИЗО
    - 🎵 Музыка
    - 💪 Физкультура
  - Каждый предмет содержит карточки учителей с:
    - Круглым фото
    - ФИО учителя
    - Классным руководством
  - Клик на карточку учителя открывает его профиль
  - Кнопка "Назад" в профиле учителя возвращает к списку по предметам

### Изменено

- Функция `handleTeachersSection()` теперь открывает список учителей
- Профиль учителя адаптирован для работы из двух разных источников:
  - Из страницы класса (возврат к классу)
  - Из списка учителей (возврат к списку)
- Кнопка "Назад" динамически меняет поведение

### Технические детали

- Добавлено модальное окно `teachers-list-modal`
- Стили для сетки учителей и карточек `.teacher-card-item`
- JavaScript функции: `generateSubjectsAccordion()`, `initializeSubjectsAccordion()`, `initializeTeacherCards()`, `openTeacherProfileFromList()`
- Mock данные с 16 учителями по разным предметам
- Динамическое обновление обработчиков кнопки "Назад"

---

## [3.3] - 2026-02-14

### Добавлено (Minor +0.5)

- **Профили учеников**:
  - Полное ФИО
  - Класс обучения
  - Фотография (кружок-аватар)
  - Кнопка "Назад" для возврата к списку класса
  - Клик на любого ученика в списке открывает его профиль

- **Профили учителей**:
  - Полное ФИО
  - Класс (классное руководство)
  - Преподаваемый предмет
  - Контактная информация (email, телефон)
  - Фотография (кружок-аватар)
  - Кнопка "Назад" для возврата к странице класса
  - Клик на карточку учителя открывает его профиль

### Изменено

- Список учеников стал кликабельным - каждый ученик открывает свой профиль
- Карточка учителя стала кликабельной
- Добавлена навигация "Назад" между окнами
- Расширена система модальных окон

### Технические детали

- Добавлены два новых полноэкранных модала: `student-profile-modal`, `teacher-profile-modal`
- Стили для профилей с круглыми фото и информационными карточками
- JavaScript функции: `openStudentProfile()`, `openTeacherProfile()`
- Обработчики кликов на элементах списка учеников и карточке учителя
- Кнопки "Назад" с анимацией и автоматическим возвратом к предыдущему окну

---

## [2.8] - 2026-02-14

### Удалено (Patch +0.1)

- **Из информации о классе удалено**:
  - ⭐ Средний балл класса (из статистики)
  - 👪 Родительский комитет (целый раздел)
  - 🏆 Достижения класса (целый раздел)
- Упрощена детальная страница класса - оставлены только основные данные:
  - Классный руководитель
  - Количество учеников, номер кабинета, староста
  - Список учеников

### Изменено

- Обновлен HTML: убраны соответствующие секции
- Упрощена функция `loadClassData()` - удалены поля `average`, `committee`, `achievements`
- Статистика класса теперь показывает только 3 карточки вместо 4

---

## [2.7] - 2026-02-14

### Изменено (Patch +0.1)

- **Список классов**: убраны буквы Е, Ж, З
- Теперь для каждой параллели доступны только классы: **А, Б, В, Г, Д, И**
- Обновлен массив `classLetters` в функции `generateGradesAccordion()`

---

## [2.6] - 2026-02-14

### Добавлено (Minor +0.5)

- **Полноэкранное окно выбора класса**:
  - При выборе уровня школы открывается полноэкранное окно
  - Accordion (выпадающие списки) для каждой параллели:
    - Начальная школа: 1-4 классы
    - Средняя школа: 5-9 классы
    - Старшая школа: 10-11 классы
  - Для каждой параллели кнопки классов: А, Б, В, Г, Д, Е, Ж, З, И
  - Плавная анимация раскрытия/закрытия accordion

- **Детальная страница класса (полноэкранная)**:
  - **Классный руководитель**: ФИО, контакты (email, телефон)
  - **Статистика класса**:
    - Количество учеников
    - Номер кабинета
    - Средний балл класса
    - Староста класса
  - **Список учеников**: нумерованный список с hover-эффектами
  - **Родительский комитет**: председатель
  - **Достижения класса**: список наград и побед

### Изменено

- Функция `handleSchoolLevelSelection()` теперь открывает окно выбора класса с accordion
- Добавлена динамическая генерация accordion на основе выбранного уровня школы
- Расширена система модальных окон для поддержки полноэкранного режима

### Технические детали

- Добавлены два новых полноэкранных модальных окна в HTML
- Стили для accordion, карточек классов, статистики и детальной информации
- JavaScript функции: `generateGradesAccordion()`, `initializeAccordion()`, `initializeClassButtons()`, `openClassDetails()`, `loadClassData()`
- Mock данные для демонстрации работы детальной страницы класса
- Адаптивная верстка для всех новых элементов

---

## [2.1] - 2026-02-14

### Добавлено (Minor +0.5)

- **Модальное окно выбора уровня школы**:
  - При нажатии на "Ученики" открывается модальное окно
  - Три варианта выбора:
    - Начальная школа (1-4 классы)
    - Средняя школа (5-9 классы)
    - Старшая школа (10-11 классы)
  - Красивые карточки с иконками и hover-эффектами
  - Закрытие по клику на overlay, кнопку X или ESC
  - Плавные анимации появления/исчезновения
  - Блокировка прокрутки при открытом модальном окне

### Изменено

- Функция `handleStudentsSection()` теперь открывает модальное окно вместо временного уведомления
- Обновлена система инициализации: добавлена `initializeModals()`
- Расширены утилиты управления UI: `openModal()`, `closeModal()`

### Технические детали

- Добавлен HTML-блок модального окна в `index.html`
- Созданы стили для `.modal`, `.modal-content`, `.school-levels`
- JavaScript обработчики для кликов по уровням школы
- Адаптивная верстка для мобильных устройств
- Accessibility: focus-visible стили для всех интерактивных элементов

---

## [1.6] - 2026-02-14

### Добавлено (Minor +0.5)

- **Выпадающее боковое меню (Sidebar)**:
  - Навигация с тремя разделами: Ученики, Учителя, Администрация
  - Плавная анимация появления слева
  - Затемненный overlay с blur-эффектом
  - Закрытие по клику на overlay, кнопку X или клавишу ESC
  - Анимированная трансформация гамбургер-меню в крестик
  - Блокировка прокрутки страницы при открытом меню

- **Новостная лента на главной странице**:
  - Вертикальная лента новостей вместо навигационных карточек
  - Карточки новостей с датами (день + месяц)
  - Краткое описание и кнопка "Читать полностью"
  - Hover-эффекты с плавным сдвигом вправо
  - 4 примера новостных статей

### Изменено

- Главная страница теперь показывает новости вместо навигационных карточек
- Навигация перенесена из основного контента в боковое меню
- Обновлена структура HTML с добавлением `<aside>` для sidebar
- Переписаны стили CSS для новой структуры
- Функция `initializeMobileMenu()` переименована в `initializeSidebar()` с расширенным функционалом

### Удалено

- Удалены три навигационные карточки из основного контента
- Удалены связанные стили для `.nav-card`, `.nav-container`
- Удалена анимация пульсации для желтой точки логотипа

---

## [1.1] - 2026-02-14

### Изменено (Patch +0.1)

- **Логотип**:
  - Изменен текст с "SEHRIYO" на "Sehriyo" (с заглавной буквы)
  - Желтая точка "●" заменена на текст "Info"
  - Добавлен класс `.logo-info` с более легким шрифтом (weight: 400)
  - Увеличено расстояние между словами в логотипе

### Удалено

- Удалена желтая точка из логотипа
- Удалены стили `.logo-dot` и анимация `@keyframes pulse`

---

## [1.0] - 2026-02-14

### Добавлено (Начальная версия)

- **Структура сайта**:
  - Создан HTML-файл с семантической разметкой
  - Адаптивный header с логотипом "SEHRIYO●"
  - Гамбургер-меню (три полоски)
  - Секция приветствия "Добро пожаловать"
  - Footer с копирайтом

- **Навигационные карточки**:
  - Три карточки: Ученики, Учителя, Администрация
  - SVG иконки для каждой карточки
  - Hover-эффекты с подъемом и тенями
  - Стрелки "→" с анимацией сдвига

- **Дизайн и стили**:
  - CSS-переменные для цветовой палитры
  - Темно-синий градиент для header (#0d2b6b → #081a42)
  - Современная типографика (шрифт Inter)
  - Плавные анимации появления элементов
  - Градиенты и тени для премиум-вида
  - Полная адаптивность для мобильных устройств

- **JavaScript функциональность**:
  - Обработчики кликов для навигационных карточек
  - Scroll animations с Intersection Observer
  - Временные уведомления "Скоро будет доступен"
  - Утилиты: debounce, smoothScroll, isInViewport
  - Поддержка модульных систем (CommonJS)

- **Accessibility**:
  - ARIA-метки для интерактивных элементов
  - Focus-visible стили для клавиатурной навигации
  - Поддержка prefers-reduced-motion
  - Семантичный HTML5

---

## Планы на будущее

### В разработке

- [ ] Полное содержимое страниц: Ученики, Учителя, Администрация
- [ ] Детальные страницы новостей
- [ ] Система управления новостями (CMS)
- [ ] Галерея фотографий школы
- [ ] Расписание уроков
- [ ] Контактная информация
- [ ] Форма обратной связи

### Рассматривается

- [ ] Мультиязычность (Русский/Узбекский/Английский)
- [ ] Темная тема
- [ ] Поиск по сайту
- [ ] Интеграция с социальными сетями
- [ ] Мобильное приложение
